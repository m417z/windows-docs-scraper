# WS_ENDPOINT_ADDRESS_DESCRIPTION structure

## Description

Information about a mapping between a [WS_ENDPOINT_ADDRESS](https://learn.microsoft.com/windows/desktop/api/webservices/ns-webservices-ws_endpoint_address) and an XML element.

## Members

### `addressingVersion`

The WS-Addressing version to use when serializing the endpoint address.