# WS_REQUEST_SECURITY_TOKEN_PROPERTY structure

## Description

Specifies a property for requesting a security token from an issuer.

## Members

### `id`

Identifies the [WS_REQUEST_SECURITY_TOKEN_PROPERTY_ID](https://learn.microsoft.com/windows/desktop/api/webservices/ne-webservices-ws_request_security_token_property_id).

### `value`

A pointer to the value.
The pointer must have an alignment compatible with the type
of the property.

### `valueSize`

The size in bytes of the memory pointed to by value.