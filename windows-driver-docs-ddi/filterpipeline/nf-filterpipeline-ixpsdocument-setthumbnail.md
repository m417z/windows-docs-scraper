# IXpsDocument::SetThumbnail

## Description

The `SetThumbnail` method removes the current thumbnail object from the document and inserts a new one.

## Parameters

### `pThumbnail` [in]

A pointer to a new thumbnail.

## Return value

`SetThumbnail` returns an **HRESULT** value.