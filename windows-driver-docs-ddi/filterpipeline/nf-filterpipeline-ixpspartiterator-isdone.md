# IXpsPartIterator::IsDone

## Description

The `IsDone` method determines whether the iterator has finished the iteration.

## Return value

`IsDone` returns **true** if the iterator has finished iterating.