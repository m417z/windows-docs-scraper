# _CO_HEADER structure

## Description

The CO_HEADER structure is used to specify values for the
**Header** member of the
[L2CAP_CONFIG_OPTION](https://learn.microsoft.com/windows-hardware/drivers/ddi/bthddi/ns-bthddi-_l2cap_config_option) structure.

## Members

### `Type`

The type of the vendor-specific option stored in the L2CAP_CONFIG_OPTION structure. This can
accept either an option or a hint.

### `Length`

The size, in bytes, of the vendor-specific option stored in the L2CAP_CONFIG_OPTION
structure.

## See also

[L2CAP_CONFIG_OPTION](https://learn.microsoft.com/windows-hardware/drivers/ddi/bthddi/ns-bthddi-_l2cap_config_option)